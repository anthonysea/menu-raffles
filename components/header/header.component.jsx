import React from 'react';

const Header = ({}) => {
	return (
		<header className="text-3xl font-bold">
			<h1>MENU Skateshop Raffles</h1>
			{/* Add an if authenticated -> render 'sign out' and 'new raffle' buttons */}
		</header>
	)
};

export default Header;